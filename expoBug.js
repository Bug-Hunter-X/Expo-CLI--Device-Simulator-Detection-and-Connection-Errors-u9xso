This error occurs when using the Expo CLI and attempting to run an app on a device or simulator that's not properly configured or connected.  It could manifest in several ways, including a generic error message or a failure to detect the device.